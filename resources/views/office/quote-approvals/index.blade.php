<x-layouts.office title="Quote approvals" width="workspace"><div class="space-y-6"><x-office.primary-toolbar title="Quote approvals" description="Pricing and terms exceptions awaiting a decision." eyebrow="Commercial" /><div class="surface overflow-hidden"><div class="divide-y divide-slate-200">@forelse($approvals as $approval)<article class="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div><p class="font-bold">{{ $approval->revision->displayNumber() }} · {{ $approval->revision->document->title }}</p><p class="mt-1 text-sm text-slate-600">{{ $approval->revision->document->opportunity->customer->display_name }} · {{ collect($approval->trigger_snapshot)->pluck('kind')->map(fn($kind)=>str($kind)->headline())->join(', ') }}</p></div><a class="button-primary" href="{{ route('office.quotes.show',[$approval->revision->document,$approval->revision]) }}#approval-publication">Review</a></article>@empty<x-office.state-panel title="No approvals waiting" message="New exception requests will appear here." />@endforelse</div></div>{{ $approvals->links() }}</div></x-layouts.office>
