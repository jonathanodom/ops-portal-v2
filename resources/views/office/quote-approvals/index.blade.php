<x-layouts.office title="Quote approvals" width="workspace"><div class="space-y-6"><header><p class="text-sm font-bold uppercase tracking-[.12em] text-brand-blue">Commercial</p><h1 class="mt-1 text-3xl font-bold">Quote approvals</h1><p class="mt-2 text-slate-600">Content-hash-bound pricing and terms exceptions awaiting a decision.</p></header><div class="surface overflow-hidden"><div class="divide-y divide-slate-200">@forelse($approvals as $approval)<article class="flex flex-col gap-4 p-5 lg:flex-row lg:items-center lg:justify-between"><div><p class="font-bold">{{ $approval->revision->displayNumber() }} · {{ $approval->revision->document->title }}</p><p class="mt-1 text-sm text-slate-600">{{ $approval->revision->document->opportunity->customer->display_name }} · {{ collect($approval->trigger_snapshot)->pluck('kind')->map(fn($kind)=>str($kind)->headline())->join(', ') }}</p></div><a class="button-primary" href="{{ route('office.quotes.show',[$approval->revision->document,$approval->revision]) }}#approval-publication">Review</a></article>@empty<div class="p-8 text-center"><h2 class="font-bold">No approvals waiting</h2><p class="mt-2 text-slate-600">New exception requests will appear here.</p></div>@endforelse</div></div>{{ $approvals->links() }}</div></x-layouts.office>
