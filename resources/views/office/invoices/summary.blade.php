<x-layouts.office :title="$invoice->invoice_number">
    <a class="inline-flex min-h-11 items-center text-sm font-bold text-brand-blue" href="{{ route('office.invoices.index') }}">&larr; Invoices</a><h1 class="mt-2 text-3xl font-bold">Invoice summary</h1>
    <div class="surface mt-6 p-5"><div class="flex flex-wrap justify-between gap-4"><div><p class="font-bold text-brand-blue">{{ $invoice->invoice_number }}</p><p class="mt-2">{{ $invoice->serviceTicket?->ticket_number ?? 'Direct invoice' }} · {{ $invoice->billing_name }}</p></div><div class="text-right"><p class="font-bold">{{ ucfirst(str_replace('_',' ',$invoice->status)) }}</p><p class="mt-2 text-2xl font-bold">${{ number_format($invoice->total_cents/100,2) }}</p></div></div><p class="mt-5 text-sm text-slate-600">Read-only billing context. Pricing controls, internal notes, presentation acknowledgments, and private documents are restricted.</p></div>
    <div class="surface mt-4 p-5"><p class="text-sm font-bold uppercase tracking-wider text-slate-500">Payment summary</p><p class="mt-2 text-xl font-bold">{{ ucfirst(str_replace('_',' ',$invoice->paymentState())) }}</p><p class="mt-1 text-sm text-slate-600">Balance ${{ number_format(max(0,$invoice->balanceCents())/100,2) }}</p></div>
</x-layouts.office>
