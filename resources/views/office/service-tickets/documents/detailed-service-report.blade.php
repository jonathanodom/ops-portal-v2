<x-layouts.print :title="'Detailed Service Report '.$ticket->ticket_number">
    @include('office.service-tickets.documents._toolbar', ['ticketId' => $ticket->id])
    <main class="print-sheet" data-print-document="detailed-service-report">
        @include('office.service-tickets.documents._header', ['profile' => 'DETAILED SERVICE REPORT', 'audience' => 'Internal operational snapshot', 'ticketNumber' => $ticket->ticket_number, 'stateLabel' => $ticket->status])
        @php($contact = $ticket->contact ?: $ticket->serviceLocation->primaryContact)
        <section class="print-section"><h2>Ticket Context</h2><div class="print-grid print-grid-2"><dl class="print-data"><dt>Customer</dt><dd>{{ $ticket->customer->display_name }}</dd><dt>Location</dt><dd>{{ $ticket->serviceLocation->name }} · {{ $ticket->serviceLocation->formattedAddress() }}</dd><dt>POC</dt><dd>{{ $contact?->name ?? '—' }}@if($contact?->role) · {{ $contact->role }}@endif</dd></dl><dl class="print-data"><dt>Priority</dt><dd>{{ Str::headline($ticket->priority) }}</dd><dt>Source / purpose</dt><dd>{{ Str::headline($ticket->source) }} · {{ Str::headline($ticket->purpose) }}</dd><dt>Billing disposition</dt><dd>{{ Str::headline($ticket->billing_disposition) }}</dd></dl></div><div class="print-prose"><h3>{{ $ticket->title }}</h3><p>{{ $ticket->description ?: 'No scope recorded.' }}</p></div>@if($ticket->customer_visible_summary)<div class="print-prose"><h3>Customer-visible summary</h3><p>{{ $ticket->customer_visible_summary }}</p></div>@endif</section>
        @if($ticket->projects->isNotEmpty())<section class="print-section"><h2>Related Projects</h2><table class="print-table"><thead><tr><th>Project</th><th>Name</th><th>Status</th></tr></thead><tbody>@foreach($ticket->projects as $project)<tr><td>{{ $project->project_number }}</td><td>{{ $project->name }}</td><td>{{ Str::headline($project->status) }}</td></tr>@endforeach</tbody></table></section>@endif
        <section class="print-section"><h2>Additional Work Items</h2>@forelse($workItems as $item)<article class="print-record"><div class="print-record-heading"><h3>{{ $item['title'] }}</h3><p>{{ Str::headline($item['status']) }} · {{ $item['origin'] === 'field_discovered' ? 'Field discovered' : 'Office added' }}</p></div>@if($item['detail'])<p class="print-preserve-lines">{{ $item['detail'] }}</p>@endif @if($item['work_note'])<p class="print-preserve-lines"><strong>Work note:</strong> {{ $item['work_note'] }}</p>@endif <p><strong>Visit / Follow-up:</strong> @if($item['discovered_visit'])Discovered {{ $item['discovered_visit'] }} · @endif {{ implode(', ', $item['handled_visits']) ?: 'No handled Visits' }}@if($item['follow_up_ticket']) · Follow-up {{ $item['follow_up_ticket'] }}@endif</p></article>@empty<p class="print-empty">No Additional Work Items.</p>@endforelse</section>
        <section class="print-section"><h2>Operational Work-Time Attribution — Not Billing Allocation</h2><table class="print-table"><thead><tr><th>Work focus</th><th>Current attributed time</th></tr></thead><tbody>@forelse($workTimeRollup as $row)<tr><td>{{ $row['label'] }}</td><td>{{ $support->duration($row['seconds']) }}</td></tr>@empty<tr><td colspan="2">No allocatable on-site or Other time.</td></tr>@endforelse</tbody></table></section>
        <section class="print-section print-page-break"><h2>Visit Crew Timelines</h2>@forelse($visitReports as $report)@php($visit = $report['visit'])<article class="print-visit-report"><div class="print-record-heading"><h3>{{ $visit->displayLabel() }}</h3><p>{{ Str::headline($visit->status) }}</p></div><div class="print-grid print-grid-3 print-compact-meta"><p><strong>Schedule</strong><br>{{ $visit->scheduledStartLocal()?->format('M j, Y g:i A T') ?? 'Unscheduled' }}@if($visit->scheduledEndLocal()) – {{ $visit->scheduledEndLocal()->format('g:i A T') }}@endif</p><p><strong>Location</strong><br>{{ $visit->serviceLocation?->name ?? $ticket->serviceLocation->name }}</p><p><strong>Crew</strong><br>{{ $visit->assignments->map(fn($a) => ($a->is_lead ? 'Lead: ' : '').$a->membership->user->name)->join(', ') ?: 'Unassigned' }}</p></div>@if($report['site_window']['start'])<p class="mt-2"><strong>Site window:</strong> {{ $report['site_window']['start']->timezone($visit->timezone)->format('M j, g:i A') }} – {{ $report['site_window']['end']->timezone($visit->timezone)->format('g:i A T') }} · {{ $support->duration($report['site_window']['duration_seconds']) }} elapsed</p>@endif
                <table class="print-table print-time-table"><thead><tr><th>Technician / Category</th><th>Effective interval</th><th>Duration</th><th>Work focus / Allocation</th><th>Review</th></tr></thead><tbody>@forelse($report['entries'] as $entry)<tr><td><strong>{{ $entry['technician'] }}</strong><br>{{ $entry['category_label'] }}@if($entry['corrected'])<br><strong>Corrected</strong>@endif</td><td>{{ $entry['started_at']?->format('M j, g:i A') }} – {{ $entry['ended_at']?->format('g:i A T') ?? 'In progress' }}@if($entry['corrected'])<br><span class="print-muted">Recorded: {{ $entry['recorded_started_at']?->format('g:i A') }} – {{ $entry['recorded_ended_at']?->format('g:i A') ?? 'In progress' }}</span>@endif</td><td>{{ $entry['duration_label'] }}</td><td>{{ $entry['captured_focus'] }}@if($entry['category'] !== 'travel')<br>@foreach($entry['allocation']['rows'] as $allocation){{ $support->duration($allocation['seconds']) }} {{ $allocation['label'] }}<br>@endforeach @if($entry['allocation']['unallocated_seconds'])<strong>{{ $support->duration($entry['allocation']['unallocated_seconds']) }} Unallocated</strong>@endif @endif</td><td>@if($entry['review'])@if($entry['review']['excluded'])Excluded @elseif($entry['review']['approved_minutes'] !== null){{ $entry['review']['approved_minutes'] }} approved min @else Factual duration accepted @endif @else—@endif</td></tr>@empty<tr><td colspan="5">No time entries.</td></tr>@endforelse</tbody></table>
                <div class="print-grid print-grid-4 print-compact-meta"><p><strong>En-route crew time</strong><br>{{ $support->duration($report['totals']['travel']) }}</p><p><strong>On-site crew time</strong><br>{{ $support->duration($report['totals']['on_site']) }}</p><p><strong>Other crew time</strong><br>{{ $support->duration($report['totals']['other']) }}</p><p><strong>Total crew time</strong><br>{{ $support->duration(array_sum($report['totals'])) }}</p></div>
                @if($includeCloseouts)<div class="print-subsection"><h3>Closeout / Review History</h3>@forelse($report['closeouts'] as $closeoutReport)@php($closeout = $closeoutReport['model'])<article class="print-record"><div class="print-record-heading"><h3>Closeout v{{ $closeout->version }}</h3><p>{{ Str::headline($closeout->status) }}</p></div>@if($closeout->submitted_at)<p><strong>Submitted:</strong> {{ $closeout->submittedBy?->name ?? 'Unknown' }} · {{ $closeout->submitted_at->timezone($visit->timezone)->format('M j, Y g:i A T') }}</p>@endif @if($closeout->outcome)<p><strong>Outcome:</strong> {{ Str::headline($closeout->outcome) }}</p>@endif @foreach(['diagnosis'=>'Diagnosis','work_performed'=>'Work performed','exceptions'=>'Exceptions','recommendations'=>'Recommendations','return_reason'=>'Return reason','unfinished_work'=>'Unfinished work','needed_equipment'=>'Needed equipment','unavailable_detail'=>'Customer unavailable detail'] as $field=>$label)@if($closeout->{$field})<p class="print-preserve-lines"><strong>{{ $label }}:</strong> {{ $closeout->{$field} }}</p>@endif @endforeach @foreach($closeout->reviews as $review)<div class="print-subsection"><p><strong>Review:</strong> {{ Str::headline($review->decision) }} · {{ $review->reviewer?->name }} · {{ $review->decided_at?->timezone($visit->timezone)->format('M j, Y g:i A T') }}</p>@if($review->reason)<p class="print-preserve-lines"><strong>Review context:</strong> {{ $review->reason }}</p>@endif</div>@endforeach @if($closeout->parts->isNotEmpty())<div class="print-subsection"><h3>Parts / Equipment</h3><table class="print-table"><thead><tr><th>Description</th><th>Quantity</th><th>Serial / MAC</th><th>Treatment</th></tr></thead><tbody>@foreach($closeout->parts as $part)<tr><td>{{ $part->description }}@if($part->technician_note)<br>{{ $part->technician_note }}@endif</td><td>{{ $part->quantity }} {{ $part->unit }}</td><td>{{ $part->serial_mac ?: '—' }}</td><td>{{ Str::headline($part->billing_treatment) }}</td></tr>@endforeach</tbody></table></div>@endif @if($closeout->media->isNotEmpty())<div class="print-subsection"><h3>Evidence Index</h3><ul>@foreach($closeout->media as $media)<li>{{ Str::headline($media->category) }} · {{ $media->caption ?: 'No caption' }} · {{ $visit->displayNumber() }} · Closeout v{{ $closeout->version }} · {{ $media->created_at->timezone($visit->timezone)->format('M j, Y g:i A T') }}</li>@endforeach</ul></div>@endif @include('office.service-tickets.documents._acknowledgment', ['acknowledgment' => $closeoutReport['acknowledgment'], 'timezone' => $visit->timezone])</article>@empty<p class="print-muted">No Closeout versions.</p>@endforelse</div>@else<p class="print-muted">Closeout narrative, evidence, reviews, and acknowledgment are restricted for this user.</p>@endif
            </article>@empty<p class="print-empty">No Visits recorded.</p>@endforelse</section>
        @if($ticket->files->isNotEmpty())<section class="print-section"><h2>Ticket Files Index</h2><table class="print-table"><thead><tr><th>File</th><th>Type</th><th>Caption</th><th>Uploader / Date</th></tr></thead><tbody>@foreach($ticket->files as $file)<tr><td>{{ $file->original_name }}</td><td>{{ $file->mime_type }}</td><td>{{ $file->caption ?: '—' }}</td><td>{{ $file->uploader?->name }} · {{ $file->created_at->timezone($activeOrganization->timezone)->format('M j, Y') }}</td></tr>@endforeach</tbody></table></section>@endif
        @if($includeHandoff || $includeInvoices)<section class="print-section"><h2>Authorized Billing Context</h2>@if($includeHandoff)<p><strong>Billing Handoff:</strong> @if($ticket->billingHandoff)#{{ $ticket->billingHandoff->id }} · {{ Str::headline($ticket->billingHandoff->status) }}@else None @endif</p>@endif @if($includeInvoices)<table class="print-table"><thead><tr><th>Invoice</th><th>Status</th><th>Issue / Due</th><th>Balance / Payment</th></tr></thead><tbody>@forelse($ticket->invoices as $invoice)<tr><td>{{ $invoice->invoice_number }}</td><td>{{ Str::headline($invoice->status) }}</td><td>{{ $invoice->issued_at?->timezone($activeOrganization->timezone)->format('M j, Y') ?? 'Not issued' }} / {{ $invoice->due_on?->format('M j, Y') ?? '—' }}</td><td>${{ number_format($invoice->balanceCents()/100, 2) }} · {{ Str::headline($invoice->paymentState()) }}</td></tr>@empty<tr><td colspan="4">No Invoices.</td></tr>@endforelse</tbody></table>@endif</section>@endif
        <footer class="print-document-footer"><span>{{ $ticket->ticket_number }}</span><span>Detailed Service Report</span><span>Internal operational snapshot</span></footer>
    </main>
</x-layouts.print>
