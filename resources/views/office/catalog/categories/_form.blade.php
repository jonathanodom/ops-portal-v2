<div class="grid gap-5"><div><label class="form-label" for="name">Category name</label><input class="form-input" id="name" name="name" value="{{ old('name',$category->name ?? '') }}" required></div><div><label class="form-label" for="code">Code</label><input class="form-input" id="code" name="code" value="{{ old('code',$category->code ?? '') }}"><p class="mt-1 text-xs text-slate-500">Leave blank to generate from the name.</p></div><div><label class="form-label" for="parent_id">Parent category</label><select class="form-input" id="parent_id" name="parent_id"><option value="">Top level</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected((string)old('parent_id',$category->parent_id ?? '')===(string)$parent->id)>{{ $parent->name }}</option>@endforeach</select><p class="mt-1 text-xs text-slate-500">Only one parent level is supported.</p></div><div><label class="form-label" for="sort_order">Sort order</label><input class="form-input" id="sort_order" name="sort_order" type="number" min="0" value="{{ old('sort_order',$category->sort_order ?? 0) }}" required></div><label class="flex min-h-11 items-center gap-3"><input type="checkbox" name="active" value="1" @checked(old('active',$category->active ?? true))><span>Active</span></label></div>
