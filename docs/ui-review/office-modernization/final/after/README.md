# Final Office UI captures

These images are reproducible running-application captures produced by `tests/Browser/office-ui-baseline.spec.js` with `OFFICE_UI_FINAL_DIR` set to this directory. They use the isolated beta Super Admin and the five viewport profiles defined in the Office UI screenshot matrix.
